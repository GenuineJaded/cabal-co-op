interface:
  display_name: "Cabal Co Op"
  short_description: "Repo-specific patterns and workflows for cabal-co-op"
  default_prompt: "Use the cabal-co-op repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true